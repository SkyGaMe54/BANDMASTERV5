---
import Layout from '../layouts/Layout.astro';

const faqs = [
  {
    question: "Qu'est-ce que BandMaster ?",
    answer: "BandMaster est un indicateur avancé conçu pour vous aider à identifier les tendances et opportunités sur les marchés financiers. Cet outil utilise des calculs spécifiques pour réduire les faux signaux et optimiser vos entrées et sorties."
  },
  {
    question: "Quel est le coût de l'indicateur ?",
    answer: "L'abonnement à BandMaster est de 150€ par mois."
  },
  {
    question: "Comment puis-je payer l'abonnement ?",
    answer: "Le paiement s'effectue par virement bancaire (IBAN) ou en crypto. Assurez-vous d'inclure votre pseudo TradingView dans les commentaires du transfert."
  },
  {
    question: "Comment obtenir l'accès après avoir payé ?",
    answer: "Une fois votre paiement confirmé, envoyez-nous votre pseudo TradingView et une preuve de paiement (capture d'écran ou justificatif)."
  },
  {
    question: "Quand dois-je renouveler mon abonnement ?",
    answer: "Le renouvellement doit être effectué chaque mois, à la date de votre premier paiement pour éviter toute interruption d'accès."
  },
  {
    question: "Existe-t-il une période d'essai ?",
    answer: "Oui, chaque nouveau client bénéficie d'une période d'essai gratuite pour tester la puissance de l'indicateur avant de se lancer."
  },
  {
    question: "Comment configurer l'indicateur sur TradingView ?",
    answer: "Un guide détaillé est disponible dans le canal Slack dédié, intitulé 'Ressources Indicateur'."
  },
  {
    question: "À qui s'adresse cet indicateur ?",
    answer: "BandMaster est adapté à tous les traders, qu'ils soient débutants ou expérimentés, souhaitant maximiser leurs résultats."
  },
  {
    question: "Quels marchés sont les plus adaptés à BandMaster ?",
    answer: "BandMaster est polyvalent et fonctionne sur de nombreux marchés : Forex, indices, matières premières, cryptomonnaies, et toutes les unités de temps."
  },
  {
    question: "Est-ce que BandMaster fournit des alertes automatiques ?",
    answer: "Oui, BandMaster inclut des alertes configurables pour signaler les croisements, retournements de tendance, et autres conditions clés."
  }
];

const categories = [
  { name: "Général", icon: "ℹ️" },
  { name: "Paiement", icon: "💳" },
  { name: "Configuration", icon: "⚙️" },
  { name: "Support", icon: "🤝" }
];
---

<Layout title="FAQ - BandMaster">
  <main class="min-h-screen bg-[#0a0a0a]">
    <!-- Hero Section -->
    <section class="py-20 relative overflow-hidden">
      <div class="absolute inset-0">
        <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_right,rgba(44,153,113,0.15),transparent_50%)]"></div>
        <div class="absolute inset-0 bg-[radial-gradient(circle_at_bottom_left,rgba(44,153,113,0.1),transparent_50%)]"></div>
      </div>
      
      <div class="container mx-auto px-4 relative z-10">
        <div class="max-w-4xl mx-auto text-center">
          <h1 class="text-5xl md:text-6xl font-bold mb-6">
            Questions <span class="text-accent">Fréquentes</span>
          </h1>
          <p class="text-xl text-muted">
            Trouvez rapidement les réponses à vos questions
          </p>
        </div>
      </div>
    </section>

    <!-- Categories -->
    <section class="pb-12">
      <div class="container mx-auto px-4">
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 max-w-4xl mx-auto">
          {categories.map(category => (
            <div class="bg-[#0f0f0f] p-4 rounded-xl text-center border border-accent/20">
              <div class="text-2xl mb-2">{category.icon}</div>
              <div class="font-semibold text-muted">{category.name}</div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- FAQ Items -->
    <section class="py-12">
      <div class="container mx-auto px-4">
        <div class="max-w-3xl mx-auto space-y-6">
          {faqs.map((faq, index) => (
            <div class="relative group" data-faq>
              <div class="absolute -inset-0.5 bg-gradient-to-r from-accent to-accent-light opacity-0 group-hover:opacity-75 transition duration-300 rounded-xl blur-lg"></div>
              <div class="relative bg-[#0f0f0f] rounded-xl overflow-hidden border border-accent/20 group-hover:border-accent/50 transition-all duration-300">
                <button
                  class="w-full text-left p-6 focus:outline-none transition-colors duration-200"
                  aria-expanded="false"
                  aria-controls={`faq-answer-${index}`}
                >
                  <div class="flex justify-between items-center">
                    <h3 class="text-xl font-semibold pr-8 group-hover:text-accent transition-colors">{faq.question}</h3>
                    <svg
                      class="w-6 h-6 transform transition-transform duration-200"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M19 9l-7 7-7-7"
                      />
                    </svg>
                  </div>
                </button>
                <div
                  class="hidden px-6 pb-6 text-muted"
                  id={`faq-answer-${index}`}
                >
                  <div class="pt-4 border-t border-accent/10">
                    <p class="group-hover:text-white/80 transition-colors">{faq.answer}</p>
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section class="py-20 relative overflow-hidden">
      <div class="absolute inset-0">
        <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(44,153,113,0.15),transparent_70%)]"></div>
      </div>
      
      <div class="container mx-auto px-4 relative z-10">
        <div class="max-w-2xl mx-auto text-center">
          <h2 class="text-3xl font-bold mb-4">Vous avez encore des questions ?</h2>
          <p class="text-muted mb-8">
            Notre équipe est là pour vous aider. N'hésitez pas à nous contacter.
          </p>
          <button
            class="chat-trigger bg-accent hover:bg-accent-light text-primary font-semibold px-8 py-4 rounded-full transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg"
          >
            Contactez-nous
          </button>
        </div>
      </div>
    </section>
  </main>
</Layout>

<script>
  // FAQ Accordion
  document.querySelectorAll('[data-faq]').forEach((faq) => {
    const button = faq.querySelector('button');
    const answer = faq.querySelector('[id^="faq-answer"]');
    const svg = button.querySelector('svg');
    
    button.addEventListener('click', () => {
      const isExpanded = button.getAttribute('aria-expanded') === 'true';
      
      // Fermer tous les autres FAQ
      document.querySelectorAll('[data-faq]').forEach((otherFaq) => {
        if (otherFaq !== faq) {
          const otherButton = otherFaq.querySelector('button');
          const otherAnswer = otherFaq.querySelector('[id^="faq-answer"]');
          const otherSvg = otherButton.querySelector('svg');
          
          otherButton.setAttribute('aria-expanded', 'false');
          otherAnswer.classList.add('hidden');
          otherSvg.style.transform = 'rotate(0deg)';
        }
      });
      
      // Basculer l'état actuel
      button.setAttribute('aria-expanded', !isExpanded);
      answer.classList.toggle('hidden');
      svg.style.transform = isExpanded ? 'rotate(0deg)' : 'rotate(180deg)';
    });
  });

  // Chat trigger
  document.querySelectorAll('.chat-trigger').forEach(button => {
    button.addEventListener('click', (e) => {
      e.preventDefault();
      if (window.Tawk_API) {
        Tawk_API.maximize();
      }
    });
  });
</script>